import React from 'react'
import styled from 'styled-components'
import { medium } from '../vars'

const IntroText = styled.div`
  font-weight: ${medium};
  line-height: 1.2;
  font-size: 20px;
`

export default IntroText
